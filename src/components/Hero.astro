<section class="relative h-[100vh] lg:h-screen overflow-hidden z-0">
  <!-- Video de fondo -->
  <video autoplay muted loop class="absolute top-0 left-0 w-full h-full object-cover z-10">
    <source src="./images/video.mp4" type="video/mp4" />
  </video>

  <!-- Overlay oscuro -->
  <div class="absolute top-0 left-0 w-full h-full bg-black/40 z-10"></div>

  <!-- Contenido (carrusel de títulos) -->
  <div class="relative z-20 flex items-start h-full px-6 lg:px-20 pt-[50vh]">

    <div
      x-data="{ active: 0, slides: [
        { title: 'CALIDAD Y RAPIDEZ' },
        { title: 'EXPERIENCIA COMO REPALDO' },
        { title: 'SEGURIDAD EN EL SERVICIO' },
        { title: 'CUIDADO EN EL TRASLADO' }
      ] }"
      x-init="setInterval(() => active = (active + 1) % slides.length, 6000)"
      class="max-w-3xl text-left relative"
    >
      <template x-for="(slide, index) in slides" :key="index">
        <div
          x-show="active === index"
          x-transition:enter="transition-all duration-1000 ease-out"
          x-transition:enter-start="opacity-0 -translate-y-8"
          x-transition:enter-end="opacity-100 translate-y-0"
          x-transition:leave="transition-all duration-700 ease-in"
          x-transition:leave-start="opacity-100 translate-y-0"
          x-transition:leave-end="opacity-0 translate-y-8"
          class="absolute"
        >
          <h1 class="titulo-principal">
            <span class="block" x-text="slide.title.split(' ')[0]"></span>
            <span class="block whitespace-nowrap"
              x-text="slide.title.split(' ').slice(1).join(' ')">
            </span>
          </h1>
        </div>
      </template>
    </div>
  </div>

  <style>
    @keyframes slideInLeft {
      0% { transform: translateX(-100px); opacity: 0; }
      100% { transform: translateX(0); opacity: 1; }
    }
    .animate-slide-in-left {
      animation: slideInLeft 1s ease-out forwards;
    }
  </style>
</section>
